================================================================
PostgreSQL Connection Information
================================================================

CONTAINER STATUS: Running and Healthy
PostgreSQL Version: 15-alpine
Port: 5432

================================================================
CONNECTION CREDENTIALS
================================================================

Username: postgres
Password: root
Database: clan_platform
Host: localhost (from outside Docker)
Port: 5432

================================================================
CONNECTION STRINGS
================================================================

For pgAdmin/DBeaver/DataGrip:
-----------------------------
Host: localhost
Port: 5432
Database: clan_platform
Username: postgres
Password: root

For Application Code (Python/SQLAlchemy):
----------------------------------------
postgresql://postgres:root@localhost:5432/clan_platform

For Docker Container to Container:
----------------------------------
postgresql://postgres:root@postgres:5432/clan_platform

================================================================
DOCKER COMMANDS
================================================================

Start PostgreSQL:
  docker-compose up -d postgres

Stop PostgreSQL:
  docker-compose stop postgres

View PostgreSQL logs:
  docker logs admin-service-postgres

Connect via psql:
  docker exec -it admin-service-postgres psql -U postgres -d clan_platform

List databases:
  docker exec -it admin-service-postgres psql -U postgres -c "\l"

================================================================
ENVIRONMENT FILES UPDATED
================================================================

All environment example files have been standardized with:
- Username: postgres
- Password: root (dev/uat) or CHANGE_ME_STRONG_PASSWORD (prod)
- Database: clan_platform

Files updated:
- config/environments/.env.local
- config/environments/.env.dev
- config/environments/.env.uat
- config/environments/.env.prod

================================================================
